interface Routes {
  home: string;
  crypto: string;
  forex: string;
  stocks: string;
  futures: string;
  advanced: string;
}

const routes: Routes = {
  home: "/",
  crypto: "/crypto",
  forex: "/forex",
  stocks: "/stock",
  futures: "/futures",
  advanced: "/advanced",
};

export default routes;
